import { Button } from '@components/Button';

import * as S from './styles';

export const Main = (): JSX.Element => {
  return (
    <S.Container>
      <h1>Main</h1>
      <Button>AAA</Button>
    </S.Container>
  );
};
